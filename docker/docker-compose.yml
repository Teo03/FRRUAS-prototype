services:
  db:
    image: postgres:16
    container_name: frruas_db
    environment:
      POSTGRES_DB: frruas_db
      POSTGRES_USER: frruas_user
      POSTGRES_PASSWORD: frruas_pass
    ports:
      - "5432:5432"
    volumes:
      - ./data:/var/lib/postgresql/data
